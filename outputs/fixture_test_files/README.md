Place exactly one Foundry `temporal_projection_source_bundle.v1` JSON fixture in this directory before running the bundled Stage C QA script.

Run:

```bat
scripts\run_chunk3_beta_3_qa.bat
```

All logs and generated artifacts are written to `outputs/fixture_outputs`.
