# Sprint Results

No slice has been executed yet. Add one durable result document for every
completed gate using the convention:

`SLICE N - Descriptive Name.md`

Compact machine-readable evidence may also be retained here. Do not store large
bounded artifacts, expanded fixtures, build outputs, environments, or caches in
this directory; record their identities, hashes, relevant counts, and concise
verification summaries instead.
