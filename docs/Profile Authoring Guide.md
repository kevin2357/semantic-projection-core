# Profile Authoring Guide

A profile contains a versioned manifest, ontology, projected term registry, object mappings, relationship mappings, context validation, and a Python implementation of the generic profile protocol.

Profiles must preserve source refs and mapping-rule refs, declare source eligibility, produce deterministic results, and avoid embedding report prose or unscoped domain assumptions.
