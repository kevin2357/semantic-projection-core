# Chunk 2.8 Repository Separation

This repository is the extracted projection layer formerly developed inside Astrology Analysis SDK. It installs independently as `semantic-projection-core` and exposes the Python package `semantic_projection`. It contains the generic engine, contracts, schemas, profiles, audits, diagnostics, projected term registries, materialization, deterministic rendering, tests, examples, and projection documentation.
