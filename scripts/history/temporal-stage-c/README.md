# Historical Stage C QA launchers

These scripts are preserved as implementation records for the numbered temporal development passes. They are not the supported QA interface and may refer to historical output locations.

Use `python scripts/run_qa.py --suite temporal` or `scripts\run_qa.bat --suite temporal` for current temporal QA. Add `--coverage` to measure branch coverage.
